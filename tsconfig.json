{
  // The workspaces carry their own tsconfigs; this one covers the handful of
  // files that live at the repo root and belong to no package.
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"]
  },
  "include": ["vitest.config.ts"]
}
